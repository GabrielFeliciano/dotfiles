This is somewhat standart NixOS system configuration repository. This repository is shared publicly, take care of secrets

There is a flake.nix and configuration.nix but packages are currently being separated in the configurations/ folder.

This repo also uses sops-nix to store secrets, for now. 
